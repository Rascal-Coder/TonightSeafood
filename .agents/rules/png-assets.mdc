---
description: 需要 PNG 时按 generate2dsprite 生成品红底原图，再用脚本导出透明 PNG
alwaysApply: true
---

# PNG 素材

角色、敌人、道具、攻击、投射物和命中特效要出 PNG 时，先读并遵循 `.agents/skills/generate2dsprite/SKILL.md`。

原图只用内置 `image_gen`。背景必须是纯色 `#FF00FF`。不要用色块、`Graphics` 矩形、Canvas、SVG 或 PIL 画角色、敌人和道具。

透明底只由技能脚本做品红抠图、切格、对齐和 QC。`--input`、`--target`、`--mode`、`--output-dir` 必填，行列和对齐按该技能选择：

```bash
python .agents/skills/generate2dsprite/scripts/generate2dsprite.py process \
  --input <raw-sheet.png> \
  --target <player|creature|projectile|impact|prop|fx> \
  --mode <idle|attack|...> \
  --rows <rows> --cols <cols> \
  --output-dir <out-dir>
```

成品是 `sheet-transparent.png` 和单帧 PNG。本项目是 Cocos，不要加 `--godot-world-height`。QC 不过就改处理参数，或重新生成原图。不要手抠，不要沿轮廓羽化，也不要把不合格的图留在工程里当占位。
