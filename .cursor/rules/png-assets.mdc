---
description: 需要图片时生成真实 PNG，抠图不抠爆并羽化边缘
alwaysApply: true
---

# PNG 素材

需要图片时直接生成真实 PNG。不要用色块、`Graphics` 矩形或纯色贴图代替角色、敌人和道具。

生成后确认两件事：

- 文件头是 `89 50 4E 47`，扩展名是 `.png`
- 主体四周是透明像素，缩进游戏里轮廓干净

背景没去掉、主体糊成一团、或文件不是 PNG 时，对这张图做抠图：只去掉背景，保留造型，输出透明底 PNG，再放进 `assets/grok/`。

抠图不要抠爆。只去掉背景，描边、触须、爪子、网线都留住，不要往主体里多擦掉一圈，也不要抠穿镂空里的造型。

边缘不要硬切。沿轮廓外侧羽化约 3 像素，用半透明过渡把锯齿抹平。羽化只在这一圈边上，主体内部保持不透明。

例如虾须被擦断、网孔被抠穿，就是抠爆；绿底去掉后，轮廓外 3 像素从透明过渡到不透明，才算合格。

抠完仍然不能用，就重新生成再抠。不要把不合格的图留在工程里当占位。
